apiVersion: v2
description: >-
  A Helm chart that collects custom resource definitions (CRDs)
  from OpenShift Console, deployed as regular templates (not via
  Helm's special crds/ directory) so they are templated and
  participate in normal upgrade/delete lifecycle.
icon: https://github.com/openshift/console/raw/master/frontend/public/imgs/logos/openshift.svg
keywords:
  - openshift-console
  - crds
kubeVersion: '>=1.16.0-0'
name: openshift-console-crds
type: application
version: 0.1.1
