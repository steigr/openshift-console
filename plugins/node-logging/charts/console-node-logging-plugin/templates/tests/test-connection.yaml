apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "console-node-logging-plugin.fullname" . }}-test-connection"
  labels:
    {{- include "console-node-logging-plugin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "console-node-logging-plugin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
