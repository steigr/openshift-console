apiVersion: v1
kind: Service
metadata:
  name: {{ include "console-terminal-plugin.fullname" . }}
  labels:
    {{- include "console-terminal-plugin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.name }}
      protocol: TCP
      name: {{ .Values.service.name }}
  selector:
    {{- include "console-terminal-plugin.selectorLabels" . | nindent 4 }}
