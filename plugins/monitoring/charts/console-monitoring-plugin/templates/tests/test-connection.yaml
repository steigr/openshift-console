apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "console-monitoring-plugin.fullname" . }}-test-connection"
  labels:
    {{- include "console-monitoring-plugin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "console-monitoring-plugin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
